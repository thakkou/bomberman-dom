# bomberman-dom

Made with our js frontend framework `mini-framework`.

## Setup

1. if working locally, change the framework path from "^0.0.1" to "<path_to_framework>", otherwise keep it untouched !

2. Run: npm i

3. Run: npx serve .